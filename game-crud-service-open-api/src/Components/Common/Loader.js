import React from "react"; // React 라이브러리 import
import "./Common.css"; // CSS 파일 import

// Loader 컴포넌트 정의
export default function Loader() {
    return (
        <div className="loader"></div> // 로딩 애니메이션을 위한 div 요소
    );
};